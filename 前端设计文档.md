# Medical-Graph-RAG 前端设计文档 (Vue 3)

## 1. 目标与范围
- 目标: 面向医生实时输入场景, 用简洁、专业化的界面呈现三层图谱优势, 强调层级结构、证据可回溯与上下文压缩。
- 运行形态: 单页 Web 应用, 支持语音与打字实时输入, 连接现有后端 API。
- 范围内: 实时输入与转写、三层图谱可视化、诊断建议流式展示、诊断链回溯。
- 范围外: 图谱构建/更新、数据导入流水线、复杂多轮会话编排。

## 2. 核心体验原则
1) 简洁专业: 控件少而明确, 主信息区聚焦图谱与证据链。
2) 三层可见: Top/Middle/Bottom 层级结构始终可感知。
3) 证据可回溯: 建议 -> 证据节点 -> 关系路径。
4) 压缩优先: Summary 先展示, 细节按需展开。

## 3. 页面结构
### 3.1 布局概览
- 顶栏: 系统标题 + 环境状态 + 入口操作。
- 左侧: 输入面板 (语音/打字、关键词建议、层级开关)。
- 中部: 图谱视图 (三层分区 + 跨层链接)。
- 右侧: 诊断建议与证据链 (流式卡片 + 回溯路径)。
- 底部: 诊断链摘要与提示条。

### 3.2 关键区域说明
- 图谱视图: 以三列/三层布局展示 Top/Middle/Bottom。
- 证据面板: 点击建议或节点, 高亮其跨层引用路径。
- Summary 面板: 固定展示当前子图 Summary, 一键展开细节节点。

## 4. 三层优势可视化设计
### 4.1 层级分区
- Top/Middle/Bottom 三个分区, 各自颜色与标签固定。
- 节点卡片显示 `layer` 和 `gid` 的缩略标识。

### 4.2 跨层引用 (Trinity)
- `REFERENCE` 关系用高亮虚线表示。
- 选中节点时显示跨层“引用数量 + 路径长度”。

### 4.3 证据回溯
- 诊断建议卡片包含证据摘要:
  - evidence_nodes: 节点名 + 层级
  - evidence_paths: 关系路径 (最多 3 条)
- 点击证据, 图谱视图聚焦并高亮路径。

### 4.4 Summary 压缩展示
- Summary 节点单独展示在右侧面板顶部。
- 展开细节时, 自动在图谱中加载补充节点并标记来源层级。

## 5. 交互流程
1) 医生语音或打字输入患者情况, 实时显示转写/输入内容。
2) 选择层级范围 (Top / Top+Middle / 全部), 触发检索与生成。
3) 后端返回子图 + Summary + 诊断建议 (流式)。
4) 图谱按层级展示, 右侧建议卡片流式补全证据。
5) 点击建议 -> 高亮证据链; 点击节点 -> 展示跨层引用。
6) Summary 展开 -> 弹出细节节点, 并显示来源层级。

## 6. 组件设计 (Vue 3)
### 6.1 页面组件
- AppShell: 顶栏 + 三栏布局容器
- InputPanel: 语音/打字输入与转写
- SearchPanel: 关键词与层级过滤
- GraphView: 三层图谱渲染
- EvidencePanel: 诊断建议 + 证据链 (流式更新)
- SummaryPanel: Summary 展示与展开控制

### 6.2 图谱组件
- LayerLane: 单层容器 (Top/Middle/Bottom)
- GraphNode: 节点卡片 (name, label, layer, gid)
- GraphLink: 关系线 (HAS_SYMPTOM/REFERENCE)
- PathHighlighter: 证据链高亮

## 7. 状态与数据结构
### 7.1 全局状态
- query: { keywords, patient_info, layers, input_mode }
- stream: { status, buffer, lastUpdate }
- graph: { nodes, links, summary, layerStats }
- suggestions: { items, selectedId }
- ui: { loading, error, highlightPath }

### 7.2 关键字段
- node.layer: "top" | "middle" | "bottom"
- node.gid: 图子图标识
- link.label: "REFERENCE" | "HAS_SYMPTOM" | ...
- summary.text: 压缩摘要

## 8. API 对接约定 (前端视角)
### 8.1 /api/graph/search
- Request: { keywords, layers, reference_hops }
- Response: { nodes, links, summary, layerStats }

### 8.2 /api/diagnosis/suggestions
- Request: { patient_info, keywords, layers, top_k, stream }
- Response: { diagnosis_suggestions, diagnosis_chain }

## 9. 实时输入与流式展示
- 语音输入: Web Speech API 或本地语音服务, 只展示转写文本。
- 打字输入: 每 1-2 秒节流更新, 避免频繁触发检索。
- 流式建议: SSE 或 WebSocket 持续更新卡片, 显示生成中状态。

## 10. 视觉与样式规范
- 配色: 深蓝 + 中灰 + 低饱和辅助色
- 字体: 思源黑体或苹方 (系统可用时)
- 图谱: 节点中等圆角, 层级色条, 关系线分类型
- 动效: 仅保留路径高亮与加载过渡

## 11. 可用性与性能
- 图谱节点数量限制: 默认 200, 超出分页加载。
- 图谱渲染: 使用虚拟渲染或分层渲染减少卡顿。
- 交互响应: 路径高亮 < 100ms。

## 12. 里程碑
1) 完成基础布局与 API 联调
2) 完成三层图谱视图与路径高亮
3) 完成证据回溯与 Summary 展示
4) 完成视觉优化与性能调优
