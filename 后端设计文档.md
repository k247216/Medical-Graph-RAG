# Medical-Graph-RAG 检索+诊断建议后端设计文档

## 1. 目标与范围
- 目标: 复用 Medical-Graph-RAG 代码, 构建单机后端, 提供知识图谱检索 + 诊断建议生成能力。
- 运行形态: 单机部署, 依赖 Neo4j 与 Qwen 模型服务。
- 范围内: 症状/疾病关键词检索, 子图返回, 检索结果增强提示, 诊断建议生成。
- 范围外: 图谱构建/更新, 数据导入流水线, 多模态(语音/图像), 复杂会话编排。

## 2. 需求概述
### 2.1 功能需求
1) 关键词检索: 输入症状/疾病关键词, 返回匹配节点及一跳/二跳关联。
2) 诊断建议: 输入患者描述与关键词, 结合检索结果生成 3-5 条诊断建议。
3) 诊断链输出: 返回由 Medical-Graph-RAG 生成的诊断链(关键节点与推理路径)。
4) 健康检查: 监测 Neo4j 与模型连通性。

### 2.2 非功能需求
- 可配置性: 通过 .env 或配置文件切换 Neo4j 与 Qwen 参数

## 3. 总体架构
```
[Client]
   | HTTP
[API Layer (Flask/FastAPI)]
   | 1) Graph Query
[Neo4j]
   | 2) Context Build
[Retriever + Prompt Builder]
  | 3) LLM Generate
[Qwen (DashScope or compatible API)]
  | 4) Diagnosis Chain Extract
[Medical-Graph-RAG]
```

### 3.1 关键流程
1) 用户提交症状/疾病关键词和患者描述。
2) 后端查询 Neo4j 子图, 得到相关疾病/症状与关系。
3) 将检索结果压缩为结构化上下文, 构建提示词。
4) 调用 Qwen 生成诊断建议, 返回结构化 JSON。
5) 基于检索结果与模型输出生成诊断链, 返回推理路径。

## 4. 模块设计
### 4.1 API 层
- 负责路由、参数校验、响应结构统一、错误处理。

### 4.2 Graph 查询模块
- Neo4j 驱动封装。
- 支持关键词模糊匹配, 取邻接节点与关系。
- 返回统一结构: nodes/links。

### 4.3 Retriever + Context Builder
- 过滤与排序: 去重、限制节点数、优先疾病节点。
- 上下文压缩: 将节点和关系拼接为简洁文本, 供 LLM 使用。

### 4.4 LLM 生成模块
- Qwen 调用封装。
- 统一 JSON 输出 schema, 严格解析。

### 4.5 诊断链构建模块
- 复用 Medical-Graph-RAG 的诊断链抽取逻辑。
- 诊断链由节点序列与关系序列组成, 支持回溯证据。

## 5. 数据模型设计
### 5.1 Neo4j 节点
- (:Disease {name, icd?, group})
- (:Symptom {name, group})
- (:Drug {name, group})
- (:Test {name, group})

### 5.2 关系
- (:Disease)-[:HAS_SYMPTOM]->(:Symptom)
- (:Disease)-[:TREATED_BY]->(:Drug)
- (:Disease)-[:RECOMMENDED_TEST]->(:Test)

### 5.3 子图输出结构
```json
{
  "nodes": [{"id": "...", "label": "Disease", "name": "...", "properties": {}}],
  "links": [{"source": "...", "target": "...", "label": "HAS_SYMPTOM", "properties": {}}]
}
```

### 5.4 诊断链输出结构
```json
{
  "diagnosis_chain": {
    "nodes": [{"id": "...", "label": "Disease|Symptom|Drug|Test", "name": "..."}],
    "links": [{"source": "...", "target": "...", "label": "HAS_SYMPTOM|TREATED_BY|RECOMMENDED_TEST"}],
    "summary": "..."
  }
}
```

## 6. API 设计
### 6.1 关键词检索
- POST /api/graph/search
- Request:
```json
{
  "keywords": "头痛, 恶心"
}
```
- Response: nodes/links 子图

### 6.2 诊断建议
- POST /api/diagnosis/suggestions
- Request:
```json
{
  "patient_info": "患者女性, 头痛伴恶心",
  "keywords": "头痛, 恶心",
  "top_k": 20
}
```
- Response:
```json
{
  "diagnosis_suggestions": [
    {"diagnosis": "偏头痛", "confidence": "高", "evidence": "症状与典型表现一致"}
  ],
  "diagnosis_chain": {
    "nodes": [{"id": "...", "label": "Disease", "name": "偏头痛"}],
    "links": [{"source": "...", "target": "...", "label": "HAS_SYMPTOM"}],
    "summary": "症状 -> 疾病 -> 推荐检查"
  }
}
```

### 6.3 健康检查
- GET /api/health
- Response:
```json
{
  "status": "ok",
  "neo4j": "ok",
  "qwen": "ok"
}
```

## 7. Prompt 设计与约束
- 只使用用户输入与检索结果, 不生成未提供的具体数值或检查结果。
- 强制 JSON 输出, 解析失败则降级为错误提示。
- 诊断链需可由检索子图回溯, 避免生成不存在的节点或关系。

示例提示词结构:
- System: 医疗辅助诊断助手, 输出严格 JSON。
- User: 患者描述 + 关键词 + 相关疾病/症状子图摘要。

## 8. 配置与依赖
- .env
  - NEO4J_URI
  - NEO4J_USER
  - NEO4J_PASSWORD
  - QWEN_API_KEY
  - QWEN_BASE_URL (可选)
- Python 依赖: flask/fastapi, neo4j, dashscope/openai compatible client

## 9. 部署与运行
- 单机部署: systemd 或 Windows 服务。
- 启动顺序: Neo4j -> 后端服务。
- 端口: 默认 8000。

## 10. 风险与对策
- 图谱稀疏: 检索结果为空时返回提示 + 引导补充症状。
- LLM 输出不规范: 采用 JSON 解析 + 回退规则。
- 误导风险: 明确说明为辅助建议, 不替代医生诊断。

## 11. 测试计划
- 单元测试: 关键词解析、子图查询、JSON 解析。
- 集成测试: Neo4j 真实连接 + Qwen 调用。
- 压力测试: 关键路径 QPS 与响应时间。

## 12. 里程碑
1) 完成 API 与 Graph 查询
2) 完成 Qwen 调用与提示词
3) 联调与验证
4) 上线部署
